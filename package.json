{
  "name": "plumline-site",
  "private": true,
  "description": "Plumline site tests, build tooling and Vite multipage pipeline. Runtime pages ship no npm deps; everything here is dev/build-only.",
  "engines": {
    "node": ">=24.15.0 <25"
  },
  "scripts": {
    "dev": "vite",
    "test": "node engine/run_all.js",
    "check": "node engine/tests_jsonld.js && node engine/tests_i18n_pages.js && node --check assets/i18n.js",
    "build": "vite build",
    "preview": "vite preview --outDir dist",
    "validate": "node engine/validate_dist.js",
    "generate:engine-mirror": "node engine/generate-engine-mirror.js --write",
    "verify": "node engine/verify_all.js",
    "generate:examples": "node engine/generate-examples.js",
    "generate:home-featured": "node engine/gen_home_featured.js"
  },
  "devDependencies": {
    "acorn": "8.18.0",
    "jsdom": "30.0.0",
    "vite": "6.4.3"
  }
}
